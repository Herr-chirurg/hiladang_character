{# templates/partials/_navbar.html.twig #}

<header class="app-header">
    
    {# 1. Titre (Gauche) #}
    <h2 class="app-title-text">
        <a href="{{ path('app_user_index') }}">Hiladang Bot</a>
    </h2>
    
    {# 2. Zone Utilisateur (Droite) #}
    <div class="user-area-container">

        <div class="username-display">{{ app.user.username ?? ""}}</div>
        
        {# 🎯 CLÉ : L'image de profil est le déclencheur. Elle est placée à l'intérieur du wrapper. #}
        <div class="menu-dropdown-wrapper">
            
            {# Ajout de la classe 'dropdown-trigger' pour le JS #}
            <img src="{{ asset('images/default_profile_image.webp') }}" 
                alt="Photo de profil" 
                class="profile-pic dropdown-trigger">
            
            {# Le menu déroulant de déconnexion #}
            <div class="dropdown-menu" id="userMenuDropdown">
                <a href="">Mon Profil</a> 
                <a href="{{ path('app_logout') }}">Déconnexion</a>
            </div>
            
        </div>
        
    </div>
</header>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    {# <a class="navbar-brand" >Mon App Symfony</a> #}
        
    <nav class="navbar">
        <ul class="nav-list">
            <li class="nav-item"><a href="{{ path('app_user_index') }}">Utilisateurs</a></li>
            <li class="nav-item"><a href="{{ path('app_character_index') }}">Personnages</a></li>
            {# <li class="nav-item"><a href="#{{ path('app_user_index') }}">Activités</a></li>  #}
            <li class="nav-item"><a href="{{ path('app_scenario_index') }}">Scenarios</a></li>
            <li class="nav-item"><a href="{{ path('app_building_index') }}">Bâtiments</a></li>
        </ul>
    </nav>

</nav>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // 🎯 CIBLEZ L'IMAGE DE PROFIL (le déclencheur)
        const profileImage = document.querySelector('.dropdown-trigger');
        
        // Sélectionne le menu déroulant
        const dropdownMenu = document.querySelector('.dropdown-menu');

        // Vérifie si les deux éléments nécessaires existent
        if (profileImage && dropdownMenu) {
            
            // 1. GESTION DE L'OUVERTURE/FERMETURE AU CLIC SUR L'IMAGE
            profileImage.addEventListener('click', function(event) {
                // Empêche le clic de se propager immédiatement à la fenêtre/document, 
                // ce qui empêcherait la ligne de fermeture (point 2) d'ouvrir le menu.
                event.stopPropagation(); 
                
                // Bascule la classe 'show' sur le menu déroulant (pour l'afficher ou le masquer)
                dropdownMenu.classList.toggle('show');
            });

            // 2. FERMER LE MENU AU CLIC À L'EXTÉRIEUR
            document.addEventListener('click', function(event) {
                // Si le clic n'est pas sur le menu déroulant ET n'est pas sur l'image elle-même, on ferme le menu.
                if (!dropdownMenu.contains(event.target) && !profileImage.contains(event.target)) {
                    dropdownMenu.classList.remove('show');
                }
            });
        }
    });
</script>