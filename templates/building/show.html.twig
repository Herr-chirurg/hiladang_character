{% extends 'base.html.twig' %}

{% block title %}Building{% endblock %}

{% block body %}
    
    {% set current_route = app.request.attributes.get('_route') %}
    {{ render(controller('App\\Controller\\ActionController::index',{'route' : current_route, 'object' : building} )) }}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ building.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ building.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td></td>
            </tr>
            <tr>
                <th>SubType</th>
                <td></td>
            </tr>
            <tr>
                <th>Production</th>
                <td></td>
            </tr>
            <tr>
                <th>Bonus</th>
                <td></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_building_index') }}">back to list</a>

    <a href="{{ path('app_building_edit', {'id': building.id}) }}">edit</a>

    {{ include('building/_delete_form.html.twig') }}
{% endblock %}
